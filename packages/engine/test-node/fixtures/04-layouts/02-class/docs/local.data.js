import { Layout } from '@rocket/engine';

// import pageTree from './pageTree.json' assert { type: 'json' };

// const tree = new ModelTree(pageTree);

export const layout = new Layout();

// {
//   header__10: () => {
//     setCurrent(tree, data.sourceRelativeFilePath);
//     return new IndexMenu().render(tree);
//   }
// }
