import { componentCatalog } from './requestTimeDemoCatalog.js';

/** @type {import('@rocket/js/types.js').JsPage} */
export default async request => {
  const url = new URL(request.url);

  return {
    demo: 'request-time JSON list',
    path: url.pathname,
    generatedAt: new Date().toISOString(),
    renderMode: 'server',
    components: componentCatalog,
  };
};

export const config = {
  path: '/request-time-javascript-pages/demo/api/components.json',
  metadata: {
    title: 'Request-time Guide Components API Demo',
    description: 'Request-time JSON output generated by a server-rendered JavaScript Page.',
  },
  render: 'server',
  menu: false,
};
