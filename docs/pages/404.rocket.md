```js server
export const config = {
  path: '/404.html',
  metadata: {
    title: 'Page not found',
    description: 'The requested Rocket documentation page could not be found.',
  },
  menu: false,
  discoverability: {
    sitemap: false,
  },
  siteHeadMetadata: {
    indexing: 'noindex',
  },
};

import { atlasNotFoundLayout } from '@rocket/js/layouts/atlasNotFound.js';
export { atlasNotFoundComponents as components } from '@rocket/js/layouts/atlasNotFound.js';
import { globalData } from './globalData.js';

export const layout = pageData => atlasNotFoundLayout(pageData, globalData);
```

# This page missed orbit

The page you asked for is not in the current flight plan. The link may be outdated, or the page may
have moved during launch prep.

[Return home](/) [Browse docs](/setup/manual-quick-start)
